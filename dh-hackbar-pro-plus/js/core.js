const Core=(()=>{let history=[],idx=-1;function extractUrlParams(urlString){try{return [...new URL(urlString).searchParams.entries()].map(([name,value])=>({name,value}))}catch{return[]}}function analyzePage(iframeDoc){const forms=[...iframeDoc.querySelectorAll('form')].map(f=>({action:f.action,method:f.method||'GET',inputs:[...f.querySelectorAll('input,textarea,select')].map(i=>({name:i.name,type:i.type||i.tagName.toLowerCase()}))}));const inputs=[...iframeDoc.querySelectorAll('input,textarea,select')].map(i=>({name:i.name,type:i.type||i.tagName.toLowerCase()}));const links=[...iframeDoc.querySelectorAll('a[href]')].map(a=>a.href);const params=extractUrlParams(iframeDoc.location.href);return{forms,inputs,params,links}}function detectTech(htmlString){const rules={WordPress:/wp-content|wp-includes/i,Drupal:/drupal/i,jQuery:/jquery/i,React:/data-reactroot|__REACT/i,Vue:/vue/i,Angular:/ng-version|ng-app/i,PHP:/\.php|PHPSESSID/i,ASP_NET:/__VIEWSTATE|aspnet/i};return Object.entries(rules).filter(([,re])=>re.test(htmlString)).map(([k])=>k)}function buildInjectedUrl(baseUrl,param,payload){const u=new URL(baseUrl);u.searchParams.set(param,payload);return u.toString()}function updateStatus(report,tech=[]){countForms.textContent=report.forms.length;countInputs.textContent=report.inputs.length;countParams.textContent=report.params.length;countLinks.textContent=report.links.length;countTech.textContent=tech.length;detectedBar.innerHTML=tech.map(t=>`<span class="tag success">${t}</span>`).join('');paramSelect.innerHTML=report.params.map(p=>`<option>${p.name}</option>`).join('');formsList.innerHTML='<h3>Forms</h3>'+report.forms.map((f,i)=>`<pre>#${i+1} ${f.method} ${f.action}\n${JSON.stringify(f.inputs,null,2)}</pre>`).join('');paramsList.innerHTML='<h3>Params</h3>'+report.params.map(p=>`<button class="payload-item">${p.name}=${p.value}</button>`).join('')}function load(url){if(!url)return;browserFrame.src=url;builderUrl.value=url;adminBaseUrl.value=url;history=history.slice(0,idx+1);history.push(url);idx=history.length-1;HBLogger.log(`Loaded ${url}`)}function bind(){document.querySelectorAll('.tabs button').forEach(b=>b.onclick=()=>{document.querySelectorAll('.tabs button,.panel').forEach(x=>x.classList.remove('active'));b.classList.add('active');document.querySelector('#'+b.dataset.tab).classList.add('active')});loadBtn.onclick=()=>load(urlInput.value);reloadBtn.onclick=()=>browserFrame.contentWindow?.location.reload();backBtn.onclick=()=>{if(idx>0)browserFrame.src=history[--idx]};fwdBtn.onclick=()=>{if(idx<history.length-1)browserFrame.src=history[++idx]};browserFrame.addEventListener('load',()=>{try{const doc=browserFrame.contentDocument;const report=analyzePage(doc);const tech=detectTech(doc.documentElement.outerHTML);updateStatus(report,tech);HBLogger.log('Page analyzed','success')}catch(e){const params=extractUrlParams(browserFrame.src);updateStatus({forms:[],inputs:[],params,links:[]},[]);HBLogger.log('Limited analysis because the frame is cross-origin','warn')}});executePayloadBtn.onclick=()=>{if(!Gate.allowed())return;const url=buildInjectedUrl(builderUrl.value,paramSelect.value,payloadInput.value);builderOutput.textContent=url;HBLogger.log(`Built authorized test URL for ${paramSelect.value}`)}}window.addEventListener('DOMContentLoaded',bind);return{analyzePage,detectTech,extractUrlParams,buildInjectedUrl,load}})();
const Gate={allowed(){const ok=document.querySelector('#authorizedToggle')?.checked;if(!ok)HBLogger.log('Action blocked until authorization is confirmed','warn');return !!ok}};window.addEventListener('DOMContentLoaded',()=>{authorizedToggle.onchange=()=>{consentState.textContent=authorizedToggle.checked?'Actions unlocked':'Actions locked';consentState.className='tag '+(authorizedToggle.checked?'success':'warn')}});
