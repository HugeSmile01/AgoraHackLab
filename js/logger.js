const HBLogger=(()=>{const logs=[];function render(){const el=document.querySelector('#log');if(!el)return;el.textContent=logs.join('\n');el.scrollTop=el.scrollHeight}function log(message,type='info'){const stamp=new Date().toISOString();logs.push(`[${stamp}] [${type.toUpperCase()}] ${message}`);render()}function exportLog(){downloadText('agorahacklab-log.txt',logs.join('\n'))}function clearLog(){logs.length=0;render()}function downloadText(name,text){const blob=new Blob([text],{type:'text/plain'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=name;a.click();URL.revokeObjectURL(a.href)}window.addEventListener('DOMContentLoaded',()=>{document.querySelector('#clearLogBtn')?.addEventListener('click',clearLog);document.querySelector('#exportLogBtn')?.addEventListener('click',exportLog)});return{logs,log,exportLog,clearLog,downloadText}})();
